<Window x:Class="DistributionDrawing.Desktop.GroundingAccessPointCreation.GroundingAccessPointCreationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="添加验电接地环" Width="430" Height="255"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
  <Grid Margin="16">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>
    <TextBlock Text="线路与实际物理方向" />
    <ComboBox x:Name="CandidateInput" Grid.Row="1" Margin="0,5,0,12"
              DisplayMemberPath="DisplayText" SelectionChanged="OnCandidateChanged" />
    <StackPanel Grid.Row="2">
      <TextBlock Text="专业线路侧" />
      <ComboBox x:Name="LineSideInput" Margin="0,5,0,0">
        <ComboBoxItem Tag="SmallerNumberSide" Content="小号侧" />
        <ComboBoxItem Tag="LargerNumberSide" Content="大号侧" />
      </ComboBox>
      <TextBlock x:Name="RecommendationText" Margin="0,6,0,0"
                 Foreground="Gray" TextWrapping="Wrap" />
    </StackPanel>
    <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
      <Button Width="78" Margin="0,0,8,0" Content="取消" IsCancel="True" />
      <Button Width="78" Content="确定" IsDefault="True" Click="OnConfirm" />
    </StackPanel>
  </Grid>
</Window>
