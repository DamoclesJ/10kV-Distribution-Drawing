<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:sys="clr-namespace:System;assembly=System.Runtime">
  <!-- Typography -->
  <sys:Double x:Key="Typography.Body">13</sys:Double>
  <sys:Double x:Key="Typography.Secondary">12</sys:Double>
  <sys:Double x:Key="Typography.Section">12</sys:Double>
  <sys:Double x:Key="Typography.Title">16</sys:Double>

  <!-- Spacing and shape -->
  <sys:Double x:Key="Spacing.Xs">4</sys:Double>
  <sys:Double x:Key="Spacing.Sm">8</sys:Double>
  <sys:Double x:Key="Spacing.Md">12</sys:Double>
  <sys:Double x:Key="Spacing.Lg">16</sys:Double>
  <sys:Double x:Key="Spacing.Xl">24</sys:Double>
  <sys:Double x:Key="ControlHeight.Compact">30</sys:Double>
  <sys:Double x:Key="ControlHeight.Normal">36</sys:Double>
  <CornerRadius x:Key="Radius.Small">3</CornerRadius>

  <!-- Neutral desktop palette. Drawing semantic colors remain separate. -->
  <Color x:Key="Color.ShellBackground">#F3F5F7</Color>
  <Color x:Key="Color.PanelBackground">#F8F9FA</Color>
  <Color x:Key="Color.CanvasBackground">#FFFFFF</Color>
  <Color x:Key="Color.Border">#D8DDE3</Color>
  <Color x:Key="Color.TextPrimary">#20252B</Color>
  <Color x:Key="Color.TextSecondary">#68717C</Color>
  <Color x:Key="Color.Accent">#246B83</Color>
  <Color x:Key="Color.AccentHover">#1F5D72</Color>
  <Color x:Key="Color.SelectedBackground">#DDECF1</Color>
  <Color x:Key="Color.HoverBackground">#E9EDF1</Color>
  <Color x:Key="Color.Disabled">#9BA3AC</Color>
  <Color x:Key="Color.Success">#287A4B</Color>
  <Color x:Key="Color.Warning">#9A6500</Color>
  <Color x:Key="Color.Danger">#B3261E</Color>

  <SolidColorBrush x:Key="ShellBackgroundBrush" Color="{StaticResource Color.ShellBackground}" />
  <SolidColorBrush x:Key="PanelBackgroundBrush" Color="{StaticResource Color.PanelBackground}" />
  <SolidColorBrush x:Key="CanvasBackgroundBrush" Color="{StaticResource Color.CanvasBackground}" />
  <SolidColorBrush x:Key="BorderBrush" Color="{StaticResource Color.Border}" />
  <SolidColorBrush x:Key="TextPrimaryBrush" Color="{StaticResource Color.TextPrimary}" />
  <SolidColorBrush x:Key="TextSecondaryBrush" Color="{StaticResource Color.TextSecondary}" />
  <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource Color.Accent}" />
  <SolidColorBrush x:Key="AccentHoverBrush" Color="{StaticResource Color.AccentHover}" />
  <SolidColorBrush x:Key="SelectedBackgroundBrush" Color="{StaticResource Color.SelectedBackground}" />
  <SolidColorBrush x:Key="HoverBackgroundBrush" Color="{StaticResource Color.HoverBackground}" />
  <SolidColorBrush x:Key="DisabledBrush" Color="{StaticResource Color.Disabled}" />
  <SolidColorBrush x:Key="SuccessBrush" Color="{StaticResource Color.Success}" />
  <SolidColorBrush x:Key="WarningBrush" Color="{StaticResource Color.Warning}" />
  <SolidColorBrush x:Key="DangerBrush" Color="{StaticResource Color.Danger}" />

  <Style x:Key="ShellPanelStyle" TargetType="Border">
    <Setter Property="Background" Value="{StaticResource PanelBackgroundBrush}" />
    <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
    <Setter Property="BorderThickness" Value="1" />
  </Style>

  <Style x:Key="ToolbarButtonStyle" TargetType="Button">
    <Setter Property="Width" Value="32" />
    <Setter Property="Height" Value="30" />
    <Setter Property="Margin" Value="1,0" />
    <Setter Property="Padding" Value="6" />
    <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
    <Setter Property="Background" Value="Transparent" />
    <Setter Property="BorderBrush" Value="Transparent" />
    <Setter Property="BorderThickness" Value="1" />
    <Setter Property="FocusVisualStyle" Value="{x:Null}" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Button">
          <Border x:Name="Chrome"
                  Padding="{TemplateBinding Padding}"
                  Background="{TemplateBinding Background}"
                  BorderBrush="{TemplateBinding BorderBrush}"
                  BorderThickness="{TemplateBinding BorderThickness}"
                  CornerRadius="{StaticResource Radius.Small}">
            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="Chrome" Property="Background" Value="{StaticResource HoverBackgroundBrush}" />
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
              <Setter TargetName="Chrome" Property="Background" Value="{StaticResource SelectedBackgroundBrush}" />
              <Setter TargetName="Chrome" Property="BorderBrush" Value="{StaticResource AccentBrush}" />
            </Trigger>
            <Trigger Property="IsKeyboardFocused" Value="True">
              <Setter TargetName="Chrome" Property="BorderBrush" Value="{StaticResource AccentBrush}" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
              <Setter Property="Foreground" Value="{StaticResource DisabledBrush}" />
              <Setter Property="Opacity" Value="0.72" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="ToolboxItemStyle" TargetType="ToggleButton">
    <Setter Property="Height" Value="38" />
    <Setter Property="Margin" Value="0,1" />
    <Setter Property="Padding" Value="10,0" />
    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
    <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
    <Setter Property="Background" Value="Transparent" />
    <Setter Property="BorderBrush" Value="Transparent" />
    <Setter Property="BorderThickness" Value="1" />
    <Setter Property="FocusVisualStyle" Value="{x:Null}" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="ToggleButton">
          <Border x:Name="Chrome"
                  Background="{TemplateBinding Background}"
                  BorderBrush="{TemplateBinding BorderBrush}"
                  BorderThickness="{TemplateBinding BorderThickness}"
                  CornerRadius="{StaticResource Radius.Small}">
            <Grid>
              <Border x:Name="Indicator"
                      Width="3"
                      Margin="2,6,0,6"
                      HorizontalAlignment="Left"
                      Background="Transparent"
                      CornerRadius="2" />
              <ContentPresenter Margin="{TemplateBinding Padding}"
                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                VerticalAlignment="Center" />
            </Grid>
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="Chrome" Property="Background" Value="{StaticResource HoverBackgroundBrush}" />
            </Trigger>
            <Trigger Property="IsChecked" Value="True">
              <Setter TargetName="Chrome" Property="Background" Value="{StaticResource SelectedBackgroundBrush}" />
              <Setter TargetName="Indicator" Property="Background" Value="{StaticResource AccentBrush}" />
              <Setter Property="Foreground" Value="{StaticResource AccentHoverBrush}" />
            </Trigger>
            <Trigger Property="IsKeyboardFocused" Value="True">
              <Setter TargetName="Chrome" Property="BorderBrush" Value="{StaticResource AccentBrush}" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
              <Setter Property="Foreground" Value="{StaticResource DisabledBrush}" />
              <Setter Property="Opacity" Value="0.7" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="ToolboxSectionStyle" TargetType="TextBlock">
    <Setter Property="Margin" Value="8,12,8,4" />
    <Setter Property="FontSize" Value="{StaticResource Typography.Section}" />
    <Setter Property="FontWeight" Value="SemiBold" />
    <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}" />
  </Style>

  <Style x:Key="InspectorLabelStyle" TargetType="TextBlock">
    <Setter Property="Margin" Value="0,8,0,4" />
    <Setter Property="FontSize" Value="{StaticResource Typography.Secondary}" />
    <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}" />
    <Setter Property="TextWrapping" Value="Wrap" />
  </Style>

  <Style x:Key="InspectorEditorStyle" TargetType="Control">
    <Setter Property="MinHeight" Value="30" />
    <Setter Property="Margin" Value="0,0,0,4" />
    <Setter Property="VerticalContentAlignment" Value="Center" />
  </Style>

  <Style x:Key="InspectorButtonStyle" TargetType="Button">
    <Setter Property="MinHeight" Value="30" />
    <Setter Property="Margin" Value="0,2,0,0" />
    <Setter Property="Padding" Value="10,4" />
  </Style>

  <Style x:Key="InspectorExpanderStyle" TargetType="Expander">
    <Setter Property="Margin" Value="0,4,0,8" />
    <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
  </Style>

  <Style x:Key="DocumentTabStyle" TargetType="TabItem">
    <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}" />
    <Setter Property="Background" Value="Transparent" />
    <Setter Property="BorderBrush" Value="Transparent" />
    <Setter Property="Padding" Value="12,0" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="TabItem">
          <Grid Background="{TemplateBinding Background}">
            <Border x:Name="Chrome" Padding="{TemplateBinding Padding}" Background="Transparent">
              <ContentPresenter ContentSource="Header" VerticalAlignment="Center" />
            </Border>
            <Border x:Name="Indicator" Height="2" VerticalAlignment="Bottom" Background="Transparent" />
          </Grid>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter Property="Background" Value="{StaticResource HoverBackgroundBrush}" />
            </Trigger>
            <Trigger Property="IsSelected" Value="True">
              <Setter Property="Background" Value="{StaticResource CanvasBackgroundBrush}" />
              <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
              <Setter TargetName="Indicator" Property="Background" Value="{StaticResource AccentBrush}" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
              <Setter Property="Foreground" Value="{StaticResource DisabledBrush}" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="DialogWindowStyle" TargetType="Window">
    <Setter Property="Background" Value="{StaticResource PanelBackgroundBrush}" />
    <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
    <Setter Property="FontSize" Value="{StaticResource Typography.Body}" />
  </Style>

  <Style x:Key="DialogFieldStyle" TargetType="Control">
    <Setter Property="MinHeight" Value="30" />
    <Setter Property="VerticalContentAlignment" Value="Center" />
  </Style>

  <Style x:Key="DialogButtonStyle" TargetType="Button">
    <Setter Property="MinWidth" Value="80" />
    <Setter Property="Height" Value="30" />
    <Setter Property="Padding" Value="12,3" />
  </Style>
</ResourceDictionary>
