# Roadmap

## V1.0

**Status:** RELEASED

V1.0 is the current stable foundation for professional electrical drawing.

## Post-V1

**Status:** FIRST STAGE CLOSED / ACCEPTED

Post-V1 remains the roadmap level; no V1.1, V1.2, or V2.0 scope is defined here.

The first confirmed implementation stage is [Post-V1 Electrical Model Closure](POST_V1_ELECTRICAL_MODEL_CLOSURE.md). Its goal is to close the principal Electrical Model gaps needed for actual 10kV work-ticket drawing and establish the unified FormatVersion 7 persistence baseline before any future Annotation / Work-ticket Presentation Layer.

The Grounding Scope Amendment is complete and frozen. The Post-EM-07 Sequencing Amendment inserts two independent amendments before interaction stabilization and establishes the confirmed Work Package order as follows:

1. WP-EM-01 Grounding Presentation Anchor Separation — Closed
2. WP-EM-02 V7 Format & Migration Foundation — Closed
3. WP-EM-03 RingCabinet Optional CableTerminal Vertical Slice — Completed / Closed
4. WP-EM-04 GroundingAccessPoint & GroundingTarget Vertical Slice — Closed / Completed
5. WP-EM-05 Grounding Layout & Interaction Closure — Closed (Windows accepted with known limitation)
6. WP-EM-06 Transformer Vertical Slice — Completed / Closed (implementation, review, Windows automated validation, and Windows professional GUI acceptance completed)
7. WP-EM-07 CustomerStation Vertical Slice — Closed
8. WP-EM-07A Transformer & Pole-Device Grounding Amendment — Closed / Archived (final accepted SHA `c852d7a1f2477628664f8aeca8bfb23cf9ee3b06`)
9. WP-EM-07B Transformer Naming Amendment — Closed / Archived
10. WP-EM-08 Electrical Model Interaction Stabilization — Closed / Accepted / Implemented / Windows Verified / Professionally Accepted; Slice A, Slice B, and Slice C are closed internal implementation slices
11. WP-EM-09 Electrical Model Closure Integration — Closed / Accepted / Implemented / Windows Verified / Professionally Accepted

WP-EM-08 Implementation Audit & Scope Revalidation passed at `c4f61653f923f985bcd8835d9735c68decee783e`, and its Scope Amendment is complete. WP-EM-08 remains one Work Package and one Codex Thread, with three independently reviewable internal implementation slices now closed. Slice A is implemented, Windows verified, professionally accepted, and closed at final accepted baseline `e84ff340cbf570d7ed6bb8b5dd7f4fc0befc6ec9`; Slice B is implemented, Windows verified, professionally accepted, and closed at `7307f1802ab8316f37020f563068390e10a470de`; Slice C is implemented, Windows verified, professionally accepted, and closed in the final chain ending at `1cb55f358e5ce18026d7ddf8911e4f67f61c05c7`. These slices do not create additional Work Packages or require separate Codex Threads:

1. **Implementation Slice A — Move Capability Closure — Closed / Accepted:** implemented direct drag for every Transformer kind and CustomerStation configuration and integrated them with group move, while preserving Pole drag, RingCabinet drag, CableTermination pole-orbit drag, and GroundingPoint symbol-offset drag. Movement changes only the corresponding persisted Layout position; all electrical, aggregate, grounding, naming, orientation, and stable identity facts remain unchanged. SwitchDevice / ordinary PoleAttachment and GroundingAccessPoint remain unavailable for independent free drag. The implementation reuses current scene rebuild and route behavior, typed move commands, and existing history / pointer lifecycle. Windows automated verification and professional acceptance passed; Fix-1 and Fix-2 were test-only corrections. It does not implement `LastValid`, a new drag transaction framework, route-family hysteresis, or routing-obstacle policy changes.
2. **Implementation Slice B — Transactional Drag Stabilization — Closed / Accepted / Implemented / Windows Verified / Professionally Accepted:** stabilized every existing production drag lifecycle with explicit `Before`, normalized `Candidate`, and transient `LastValid`; updated `LastValid` only after Layout validation and complete scene rebuild; rolled expected invalid candidates back to `LastValid` while keeping the gesture active; provided non-modal feedback; committed `LastValid` on release; restored `Before` on Cancel; and kept drag-generated CommandStack history, Layout, and scene atomic across commit, Undo, and Redo. Coverage includes `DeviceDragController`, `GroundingPointDragController`, and `CableRouteDragController` without adding draggable objects. Expected candidate invalidity remains distinct from unexpected invariant failure. Main implementation is `502851c0ac4e21da1eec3a6d82fb8334b1b3a483`; Windows Fix-1 is `09e6e2d3c5648d9fea1e46ffb302e4bbbddb5c27`; Windows Fix-2 is `7307f1802ab8316f37020f563068390e10a470de`. This slice does not implement route-family hysteresis, `RouteFamilyKey`, routing-obstacle policy, a generic router rewrite, or a new collision model.
3. **Implementation Slice C — Route Continuity Stabilization — Closed / Accepted / Implemented / Windows Verified / Professionally Accepted:** implemented routing/runtime-internal `RouteFamilyKey`, transient continuity state independently scoped by `ConnectionId`, two-phase accepted-state publication, and hysteresis only among legal, sufficiently close route families. The calibrated 8 mm route-family switching margin yields stable legal route selection while higher-priority formal constraints, including `PreferredHorizontalY`, override hysteresis. Unrelated Transformer and CustomerStation professional body envelopes are hard routing obstacles; endpoint owners exclude their own obstacle by stable identity. The implementation preserves GAP / OHL required-waypoint behavior, group all-or-none publication, and final release publish before `EndGesture`. Clipboard integration closure adds deterministic bounded fallback for routing-invalid default placement, with atomic rollback, rematerialized retries, repeated-paste advancement, `PasteAt` isolation, and Copy reset. This remains obstacle-aware route selection, not generic drag-collision legality.

WP-EM-08 is Closed / Accepted / Implemented / Windows Verified / Professionally Accepted. It remains one Work Package and one Codex Thread; Slice A, Slice B, and Slice C are internal increments only. WP-EM-09 is Closed / Accepted / Implemented / Windows Verified / Professionally Accepted at final implementation baseline `bd8147956a0c4fba0acb0b47067c64f819d2507d`.

WP-EM-09 completed Option A — Test-only Integration Closure and the separately reviewed Option B CustomerStation Selection Deletion Fix-1. P1-1 through P1-8 and P2-1 through P2-4 coverage dispositions are complete. Windows automated verification and GUI professional acceptance passed. The Option B fix folds a selected CustomerStation-owned `IsolationSwitch` into deletion only when its formal owner parent is also selected; standalone owned-switch protection, external dependency guards, `CompositeDeleteCommand` atomicity, and stable-identity Undo / Redo remain intact. It is an integration defect fix, not a new bulk-delete or whole-document-clear capability. Persistence schema changes, V8, new migrations, and new electrical-model capabilities remain excluded; FormatVersion remains V7.

Two professional-acceptance findings remain outstanding and were not fixed or accepted as normal behavior in this stage. (1) Device drag and CableRoute drag are reported noticeably slow at approximately three to four switchgear stations; code audit confirmed full-scene, all-Connection routing and WPF drawing rebuild per valid drag candidate, while Windows phased timing and incremental/localized optimization remain outstanding. (2) PNG export is reported to fail at default resolution for a drawing with approximately four switchgear stations and four to five poles; default export is 300 DPI with per-dimension and total-pixel budget guards, but actual project bounds / pixel count are unknown, so the trigger is not conclusively characterized. Automatic readability-first resolution adaptation with safe pixel / memory protection remains follow-up scope.

The Post-V1 Electrical Model Closure stage is Closed / Accepted for its frozen scope only. Subsequent work returns to project-level Post-V1 Requirement Reassessment / Planning to prioritize drawing interaction performance, PNG automatic resolution adaptation / readability, and the future Annotation / Work-ticket Presentation Layer. This is a planning proposal for project-control review, not an approved sequence or newly created Work Package. This closure does not create WP-EM-10, begin Annotation or Energization, resolve all performance issues, implement automatic PNG resolution adaptation, or define V1.1, V1.2, or V2.0.

WP-EM-08 has no persistence schema impact and keeps FormatVersion V7. `LastValid`, invalid candidates, feedback, `RouteFamilyKey`, route-continuity snapshots, hysteresis, and transaction state remain transient and never enter Domain, persisted DTOs, commands, or Undo / Redo history. Slice C preserves Slice B rollback semantics, real OHL + GAP required waypoint / stub / no-backtracking behavior, and deterministic formal rebuild after Undo / Redo or Save / Open. Its automated matrix and Windows professional acceptance cover single / multi-obstacle continuity, switching boundaries, device-body bypass, endpoint-owner exclusion, all three drag controllers, group all-or-none behavior, invalid continuation, and existing guide / crossing / overlap ranking. The accepted WP-EM-05 RingCabinet above-terminal grounding presentation limitation remains outside WP-EM-08. WP-EM-09 remained Integration-only and did not introduce Transformer drag, CustomerStation drag, `LastValid`, hysteresis, or other new business capability for the first time.

WP-EM-07A and WP-EM-07B are Post-EM-07 / Pre-EM-08 amendment Work Packages. They do not reopen WP-EM-06 or WP-EM-07. This ordering does not define a new release version. No V1.1, V1.2, or V2.0 release scope is defined; Annotation and Energization remain Post-V1 candidates and are outside the closed Electrical Model Closure stage.

## Post-V1 Drawing Performance & PNG Export Stabilization

**Status:** ACTIVE — WP-PERF-01 CLOSED / ACCEPTED; WP-PNG-01 IN PROGRESS

This stage follows, and does not reopen, the Closed / Accepted Post-V1 Electrical Model Closure stage or WP-EM-01 through WP-EM-09. Its baseline is closure commit `841ca66319948661f7a4aca9202247d479788d7b`; `FormatVersion` remains V7. It creates no WP-EM-10 and defines no V1.1, V1.2, or V2.0 release.

The approved order and boundary are:

1. **WP-PERF-01 — Drawing Interaction Performance — Closed / Accepted.** WP-PERF-01A, WP-PERF-01B, WP-PERF-01C-H1, WP-PERF-01C-H2, WP-PERF-01C-H3-1, WP-PERF-01C-H3-2A, WP-PERF-01C-H3-2B, and WP-PERF-01C-H3-2C-1 are Accepted. Release solution build, the complete Windows test suite, H3-2C-1 differential verification, performance tracing, and actual Device / CableRouteGuide professional use passed. No H3-2C-2 is created. Compact Adjacency, Incremental Routing, Scene partial rebuild, MouseMove throttle / debounce, and other deep Router changes remain Deferred / Not Authorized. The accepted stop condition is preservation of professional behavior while normal- and pressure-scale work drawings reach a practical, acceptable interaction experience; further Router work currently has an insufficient benefit-to-risk ratio.
2. **WP-PNG-01 — PNG Export Stabilization — In Progress.** WP-PNG-01A Automatic Safe DPI & PNG Export Sizing is Implementation Complete / Windows Calibration Finalization. It automatically selects the highest safe integer DPI up to 300 using fixed, deterministic pixel, stride, and estimated-working-memory budgets while preserving the existing millimeter bounds, 10 mm margin, white background, WPF rendering, PNG encoding, and temporary-file replacement path. Windows real-drawing calibration accepted exports at 136 DPI and 100 DPI; the pressure drawing establishes the frozen `MinimumReadableDpi = 100`. The 32M-pixel, 384 MiB, 3× peak-surface, and 64 MiB headroom values remain unchanged because current evidence supports neither relaxing nor tightening them. Final closure still requires the Windows automated rerun and GUI confirmation that successful export visibly reports its selected DPI. No routine DPI selector, vector/tiled/multipage export, or asynchronous export rewrite is included.

Approximately 60 FPS remains an interaction design target rather than a formal acceptance threshold and was not frozen by WP-PERF-01 closure. Annotation, Energization Analysis, and work-ticket assisted authoring are outside this stage.

WP-EM-04 closed at implementation baseline `b15166c96bcf03a38acd2a27d98b597d04b60d4d` after implementation, review, automated validation, Windows runtime validation, and GUI acceptance. WP-EM-05 is now Closed after implementation, focused review, automated verification, and Windows GUI acceptance with one accepted, deferred RingCabinet above-terminal presentation limitation. WP-EM-06 is also Closed after implementation, review, Windows automated validation, and Windows professional GUI acceptance. WP-EM-07 is Closed after implementation, code review, Windows automated verification, and Windows professional visual acceptance. WP-EM-07A is Closed / Archived after implementation, targeted review, Windows automated verification, and Windows professional acceptance at `c852d7a1f2477628664f8aeca8bfb23cf9ee3b06`; its additive V7 amendment introduces `GroundingAccessPlacementSide` and `TransformerSide` without V8. WP-EM-07B is Closed / Archived after implementation, Windows automated verification, and professional acceptance. Its single naming fact is required `Device.DisplayName`, shown as “变压器名称”; the additive document-level `TransformerNamingContractVersion` discriminator keeps `FormatVersion = V7` while separating legacy compatibility mode from current strict naming mode. Legacy incomplete state must be completed before Save / Save As. Generic drag and routing stabilization remains assigned to WP-EM-08, while final integration and regression remain assigned to WP-EM-09.
