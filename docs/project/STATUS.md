# Current Status

- **Project:** 10kV Distribution Drawing
- **Current Release:** v1.0.0
- **Release State:** Released
- **V1.0 baseline:** `985b6c2cd9a1c0648048f87bf50509d517045bbd`
- **FormatVersion:** V8 (WP-WTA-01 Windows acceptance candidate; released V1.0 remains V7)
- **WP-WTA-01:** Implementation Complete / Code Review Passed / Awaiting Windows Acceptance. V8 requires WorkTicketData. Versions below and above V8 are rejected; no historical project migration is provided. See [WP-WTA-01 Fix Report](WP-WTA-01-FIX-REPORT.md). Automated verification is blocked on macOS because `Microsoft.WindowsDesktop.App` is unavailable. WP-WTA-01 remains Open; it is not Closed.
- **Current Development Target:** Post-V1 Drawing Performance & PNG Export Stabilization
- **Completed Work Packages:** WP-EM-01 Grounding Presentation Anchor Separation — Closed; WP-EM-02 V7 Format & Migration Foundation — Closed; WP-EM-03 RingCabinet Optional CableTerminal Vertical Slice — Closed; WP-EM-04 GroundingAccessPoint & GroundingTarget Vertical Slice — Closed; WP-EM-05 Grounding Layout & Interaction Closure — Closed (Windows accepted with known limitation); WP-EM-06 Transformer Vertical Slice — Closed; WP-EM-07 CustomerStation Vertical Slice — Closed; WP-EM-08 Electrical Model Interaction Stabilization — Closed / Accepted / Implemented / Windows Verified / Professionally Accepted; WP-EM-09 Electrical Model Closure Integration — Closed / Accepted / Implemented / Windows Verified / Professionally Accepted; WP-PERF-01 Drawing Interaction Performance — Closed / Accepted / Windows Verified; WP-PNG-01 PNG Export Stabilization — Closed / Accepted / Windows Verified
- **Grounding Scope Amendment:** Completed / Frozen
- **WP-EM-07A State:** Closed / Archived / Final accepted implementation `c852d7a1f2477628664f8aeca8bfb23cf9ee3b06`
- **Post-EM-07 Sequencing Amendment:** Completed / Frozen
- **Current Work Package:** WP-PNG-01 PNG Export Stabilization — Closed / Accepted / Windows Verified; WP-PNG-01A Automatic Safe DPI & PNG Export Sizing — Accepted / Windows Verified
- **Internal Implementation Plan:** WP-EM-08 Slice A — Move Capability Closure — Closed / Accepted / Implemented / Windows Verified / Professionally Accepted; WP-EM-08 Slice B — Transactional Drag Stabilization — Closed / Accepted / Implemented / Windows Verified / Professionally Accepted; WP-EM-08 Slice C — Route Continuity Stabilization — Closed / Accepted / Implemented / Windows Verified / Professionally Accepted
- **WP-EM-09 Implementation Mode:** Closed — Option A Test-only Integration Closure, with approved Option B CustomerStation Selection Deletion Fix-1
- **WP-EM-08 Governance:** Implementation Audit & Scope Revalidation passed at `c4f61653f923f985bcd8835d9735c68decee783e`; Scope Amendment completed; Slice A is implemented, Windows verified, professionally accepted, and closed at `e84ff340cbf570d7ed6bb8b5dd7f4fc0befc6ec9`; Slice B is implemented, Windows verified, professionally accepted, and closed at `7307f1802ab8316f37020f563068390e10a470de`; Slice C is implemented, Windows verified, professionally accepted, and closed in the final implementation chain ending at `1cb55f358e5ce18026d7ddf8911e4f67f61c05c7`. WP-EM-08 is **Closed / Accepted / Implemented / Windows Verified / Professionally Accepted**. FormatVersion remains V7. WP-EM-08 remains one Work Package and one Codex Thread; its three slices are internal implementation increments, not additional Work Packages.
- **Blockers:** None for the Closed / Accepted Post-V1 Drawing Performance & PNG Export Stabilization stage. WP-PERF-01 and WP-PNG-01 are Closed / Accepted / Windows Verified.

V1.0 provides a standard portable profile and a legacy Windows 10 portable profile. The legacy profile is for older Windows 10 systems that cannot be upgraded or adequately serviced. V1.0 has completed target-machine validation.

## Post-V1 Scope

Post-V1 Electrical Model Closure is Closed / Accepted. Project control must conduct a new Post-V1 Requirement Reassessment / Planning. No V1.1, V1.2, or V2.0 release scope is defined.

The **Post-V1 Drawing Performance & PNG Export Stabilization** stage is Closed / Accepted / Windows Verified. It preserves the closed Electrical Model baseline at closure commit `841ca66319948661f7a4aca9202247d479788d7b` and keeps `FormatVersion = V7`. WP-PERF-01 remains Closed / Accepted / Windows Verified. WP-PNG-01A is Accepted / Windows Verified, and WP-PNG-01 is Closed / Accepted / Windows Verified after the Windows automated suite and professional real-drawing acceptance. The frozen PNG policy is `MaxRequestedDpi = 300`, `MinimumReadableDpi = 100`, `MaxDimensionPixels = 32768`, `MaxPixels = 32,000,000`, `BytesPerPixel = 4`, `PeakSurfaceFactor = 3.0`, `FixedHeadroomBytes = 64 MiB`, `MaxExportWorkingBytes = 384 MiB`, and `MaxStrideBytes = int.MaxValue`. Export prefers 300 DPI, otherwise selects the highest safe integer DPI in the continuous 100–299 range; if 100 DPI is unsafe it fails explicitly without querying live machine RAM or retrying after OOM at a lower DPI. Windows acceptance verified ordinary export at 300 DPI, the former failure drawing at approximately 136 DPI (`6160 × 4486`), the pressure drawing at 100 DPI (`8382 × 3299`) with professional readability acceptance, and correct rejection of a still-larger drawing at the frozen 100-DPI floor. Selected-DPI feedback, PNG metadata and pixel dimensions, absence of new text or geometry clipping, and the temporary-file-before-target-replacement protection contract were verified. PNG below 100 DPI, tiled export, multi-page PNG, SVG, PDF/vector export, and background STA / asynchronous export are Deferred and require independent future Work Packages. Annotation, Energization Analysis, and work-ticket assisted authoring remain outside this stage. No WP-EM-10 is created and no V1.1, V1.2, or V2.0 release is defined.

The prior README format-version backlog is superseded by WP-WTA-01's V8 development baseline. Historical V7 references below describe closed work packages, not the current file format.

WP-EM-01, WP-EM-02, WP-EM-03, WP-EM-04, WP-EM-05, WP-EM-06, and WP-EM-07 are complete following code review, automated verification, and Windows validation. WP-EM-03 completed the RingCabinet optional cable-terminal vertical slice, including nullable V7 persistence, dependency protection, clipboard, rendering, and interval internal-lead preservation when the cable terminal is absent. WP-EM-04 completed the GroundingAccessPoint and typed GroundingTarget vertical slice. WP-EM-05 completed GroundingPoint layout, drag, leader presentation, target affordance, persistence, lifecycle, and Canvas / PNG consistency. WP-EM-06 completed the Transformer vertical slice, including its three professional glyphs, electrical and command foundation, V7 persistence, desktop workflow, and Windows professional acceptance. WP-EM-07 completed the CustomerStation vertical slice, including the formal aggregate, one/two feeder topology, V7 persistence, Clipboard, professional presentation, creation / Inspector workflow, and Windows professional acceptance. WP-EM-08 is now closed after implementation, Windows verification, and professional acceptance for all three internal slices. The Post-V1 Grounding Scope Amendment, WP-EM-04 closure, WP-EM-05 closure, WP-EM-06 closure, WP-EM-07 closure, and WP-EM-08 closure are recorded in [Post-V1 Electrical Model Closure](POST_V1_ELECTRICAL_MODEL_CLOSURE.md). Do not enter later Work Packages or the future Annotation / Work-ticket Presentation Layer implicitly.

The Post-EM-07 Sequencing Amendment inserts two independent Post-EM-07 / Pre-EM-08 Work Packages without reopening WP-EM-06 or WP-EM-07. WP-EM-07A is Closed / Archived after implementation, review, Windows automated verification, and Windows professional acceptance at `c852d7a1f2477628664f8aeca8bfb23cf9ee3b06`. The Windows Professional Acceptance Amendment adds `GroundingAccessPlacementSide` (`PoleSide | AdjacentEndpointSide`) and `GroundingAccessLineSide.TransformerSide`, allowing two independent conductor GAP identities on the same legal pole-mounted Transformer short OHL. V7 remains additive-compatible: old records without placement normalize to `PoleSide`; no V8 migration is authorized. WP-EM-07B is Closed / Archived after implementation, Windows automated verification, and professional acceptance: all three Transformer kinds use the single required `Device.DisplayName`, presented in Chinese as “变压器名称”; the additive document-level `TransformerNamingContractVersion` discriminator distinguishes pre-WP-EM-07B V7 compatibility mode from current strict V7 naming mode. Legacy incomplete Transformers cannot be saved until all names are completed. WP-EM-08 is Closed / Accepted / Implemented / Windows Verified / Professionally Accepted for Slice A, Slice B, and Slice C. WP-EM-09 is Closed / Accepted / Implemented / Windows Verified / Professionally Accepted at final implementation baseline `bd8147956a0c4fba0acb0b47067c64f819d2507d`.

### WP-EM-09 Requirements Freeze (Historical)

- Purpose: complete final cross-feature integration, V7 Save / Open, Clipboard / Undo / Redo, dependency lifecycle, transient-state non-persistence, Windows runtime validation, and professional acceptance for the Post-V1 Electrical Model Closure stage.
- P1 matrix: Pole / SwitchDevice / OHL / Transformer + GAP + GroundingPoint; RingCabinet cable connections to PublicIndoor Transformer, BoxStation, and dual-feeder IndoorStation; multi-device obstacles with `CableRouteGuide`, drag, and continuity; mixed Pole / Transformer / CustomerStation / RingCabinet group move; Clipboard bounded-fallback exhaustion; and full Post-V1 Save → Close → Open.
- P2 matrix: RingCabinet ordinary interval without `CableTerminal`; explicit serialized-payload transient-state absence; V6 → V7 upgrade followed by modern-device Save / Open; and mixed dependency-deletion atomic rollback. Every P2 item requires an implemented, already-covered, or reasoned-deferred disposition before closure.
- Persistence schema impact is none expected; `FormatVersion` remains V7. `LastValid`, invalid Candidate, `RouteFamilyKey`, continuity snapshots, hysteresis state, and drag transaction state remain transient and must not be persisted.
- Closure requires complete P1 and P2 disposition, all five test projects passing with skipped = 0, Windows GUI / professional acceptance, no unresolved P0/P1 integration defect, no unauthorized capability, and completed governance closure.
- After WP-EM-09 closes, the Post-V1 Electrical Model Closure stage ends. Further work returns to project-level Post-V1 Requirement Reassessment / Planning; this Work Package does not create WP-EM-10 or begin Annotation, Energization, or release-version definition.

### WP-EM-09 Final Closure

- Final implementation baseline: `bd8147956a0c4fba0acb0b47067c64f819d2507d`. Option A — Test-only Integration Closure completed; P1-1 through P1-8 and P2-1 through P2-4 coverage dispositions are complete. Windows automated verification and GUI professional acceptance passed; the user confirmed CustomerStation deletion GUI acceptance.
- Integration coverage includes RingCabinet optional-terminal combinations; Transformer and CustomerStation; GroundingPoint / GAP; Cable / OHL connections; routing and interaction; mixed-device group move; Clipboard bounded fallback; Undo / Redo; V6 → V7 compatibility; full Post-V1 Save / Close / Open; transient interaction-state non-persistence; and dependency-deletion atomicity. P1-7 acceptance proves exhaustion of all eight candidates, exception propagation, atomic state rollback, and no automatic placement sequence advance. “First seven fail, eighth succeeds” is not a closure claim.
- Approved Option B fix: `SelectionDeletePlanner` resolves the formal CustomerStation owner identity for a selected incoming `IsolationSwitch`, folds that switch into deletion only when its exact parent is selected, retains standalone owned-switch protection, does not fold a switch belonging to another unselected station, preserves external dependency guards, and reuses `CompositeDeleteCommand` atomicity and stable-identity Undo / Redo. Commit: `bd8147956a0c4fba0acb0b47067c64f819d2507d`. This is an integration defect fix, not a new bulk-delete or whole-document-clear capability.
- Drag performance remains a known outstanding issue: the user reports noticeable lag in device drag and CableRoute drag at approximately three to four switchgear stations. Code audit confirmed that each valid drag candidate triggers full-scene, all-Connection routing rebuild and WPF drawing rebuild. Windows phased timing, incremental affected-Connection routing, localized Scene / WPF updates, and performance acceptance remain outstanding. Deferred to project-level reassessment / dedicated performance scope; not fixed or accepted as normal behavior.
- PNG export remains a known outstanding issue: the user reports default-resolution export failure with approximately four switchgear stations and four to five poles. Existing export uses 300 DPI by default and has per-dimension and total-pixel budget guards; actual project bounds / pixel count have not been obtained, so budget preflight versus memory exhaustion is not conclusively characterized. Follow-up direction is default automatic resolution adaptation prioritizing readability, highest safe resolution, a minimum readability threshold, safe pixel / memory protection, and no routine manual DPI selection. Deferred to project-level reassessment / dedicated export-improvement scope; not fixed or accepted as normal limitation.
- The Post-V1 Electrical Model Closure stage is Closed / Accepted for its frozen scope only. FormatVersion remains V7. This does not mean all performance issues are solved, PNG automatic resolution adaptation is implemented, or a new release is defined. Project control must reassess and prioritize (1) drawing interaction performance, (2) PNG automatic resolution adaptation and readability, and (3) the future Annotation / Work-ticket Presentation Layer. This order is a planning proposal, not an approved schedule or newly created Work Package. Do not create WP-EM-10 implicitly.

### WP-EM-08 Requirements Refinement and Slice A Closure

- Purpose: complete formal Device move capability, transactional drag legality, transient `LastValid`, non-modal drag feedback, route-presentation continuity, and cross-device regression without creating a generic routing framework.
- Move policy: Pole and RingCabinet support direct drag; CableTermination retains pole-orbit drag; GroundingPoint retains symbol-offset drag; Transformer and CustomerStation direct drag are assigned to WP-EM-08 Slice A. SwitchDevice / ordinary PoleAttachment remain unavailable for independent free drag and continue to move with their parent Pole or through existing attachment layout / rotation / property commands. GroundingAccessPoint remains derived from its typed electrical route and is not directly draggable.
- Illegal geometry is limited to provable Layout / Routing invalidity such as non-finite coordinates, loss of two distinct route points, insufficient required-stub capacity, impossible required direction / no-backtracking constraints, scoped routing invariants, and typed layout invariant violations. Overlap, canvas bounds, screen-coordinate Pole ordering, generic collision, and route crossing are not new business-invalid rules.
- `LastValid`, invalid candidates, drag feedback, route-family identity, hysteresis cache, and drag transaction state are runtime interaction facts and are not persisted. Invalid candidates keep the gesture active and restore or retain `LastValid`; release commits `LastValid`, while a gesture with no legal movement creates no history entry.
- Internal implementation slices: Slice A — Move Capability Closure; Slice B — Transactional Drag Stabilization; Slice C — Route Continuity Stabilization. They are independently reviewable increments within the single WP-EM-08 Work Package and do not require separate Codex Threads. Slice C characterization reproduced unnecessary route-family switching and froze transient per-Connection continuity requirements; implementation calibrated the route-family switching margin to 8 mm and verified it through automated and Windows visual acceptance.
- Slice A closure: direct drag and group-move participation for all Transformer kinds and all CustomerStation configurations is implemented by changing only `TransformerLayout.Position` or `CustomerStationLayout.Position`. It reuses the existing Canvas pointer lifecycle, `DeviceDragController`, typed commands `MoveTransformerCommand` / `MoveCustomerStationCommand`, `GroupMoveCommand`, `CommandStack`, and `RefreshDrawingScene()` path. Stable Device, Terminal, feeder, switch, connection, grounding, topology, naming, and orientation identities and facts are preserved.
- Slice A is **Closed / Accepted**: direct-drag, preview, live anchor / connected-route / grounding-presentation rebuild, Cancel-to-`Before`, Commit, Undo / Redo, selection / hit-test, group move, persistence round-trip, unsupported Switch / GAP drag regressions, Windows automated verification, and Windows professional acceptance passed. Fix-1 and Fix-2 were test-only verification corrections. Slice A does not introduce `LastValid`, invalid-candidate continuation, non-modal invalid feedback, transaction/history redesign, route hysteresis, `RouteFamilyKey`, obstacle policy, collision or bounds rules, waypoint editing, or router rewrite.
- Slice A closure baselines: Requirements Freeze `3e4dfe5fbada919b85fa1c84c1d1796f8376955e`; implementation `bfc551d358458433f37e517bf45e28138c24987d`; Windows Fix-1 `4bb4887e6a75f56756ab45f48820ad799b396ec6`; Windows Fix-2 / final accepted Slice A `e84ff340cbf570d7ed6bb8b5dd7f4fc0befc6ec9`. Persistence schema impact is none; FormatVersion remains V7.
- Slice B Requirements Freeze: stabilize all existing production drag gestures through a transient `Before` / `Candidate` / `LastValid` model. A candidate becomes `LastValid` only after Layout mutation, existing validation, and complete scene rebuild all succeed. Expected invalid candidates roll the complete Layout / scene back to `LastValid`, keep the gesture active, provide non-modal feedback, and never enter history; release commits `LastValid`, while Cancel always restores `Before`.
- Slice B covers `DeviceDragController` (Pole, RingCabinet, CableTermination, Transformer, CustomerStation, group move), `GroundingPointDragController`, and `CableRouteDragController`. It freezes transactional preview, commit, Undo, and Redo consistency for their drag-generated commands without extending the move-capability matrix or rewriting the application-wide command architecture.
- Slice B requires explicit classification between expected candidate invalidity and unexpected programming / structural invariant failure. It adds no overlap, canvas-bounds, visual-order, collision, or route-crossing legality; route-family hysteresis, `RouteFamilyKey`, route continuity cache, and Transformer / CustomerStation obstacle policy remain Slice C scope. Persistence impact is none and FormatVersion remains V7.
- Persistence schema impact is expected to be none. FormatVersion remains V7. WP-EM-09 remains Integration-only and cannot absorb first implementation of WP-EM-08 capabilities.

### WP-EM-08 Slice B Closure Evidence

- Status: **Closed / Accepted / Implemented / Windows Verified / Professionally Accepted**.
- Implementation commit: `502851c0ac4e21da1eec3a6d82fb8334b1b3a483` — `feat(interaction): stabilize transactional drag lifecycle`.
- Windows Verification Fix-1: `09e6e2d3c5648d9fea1e46ffb302e4bbbddb5c27` — `test(interaction): fix Slice B Windows routing fixture`.
- Windows Verification Fix-2: `7307f1802ab8316f37020f563068390e10a470de` — `test(interaction): tolerate Slice B fixture rounding`.
- Accepted behavior includes the `Before` / `Candidate` / `LastValid` transactional lifecycle, expected-invalid rollback with active gesture continuation and non-modal feedback, invalid-release commit of `LastValid`, no-change release, Cancel restoration to `Before`, typed `DragCandidateConstraintException` and `RoutingConstraintException`, unexpected modal/error boundary, atomic Execute / Undo / Redo recovery, Layout / Scene / history consistency, and all-or-none group rollback.
- Windows automated verification and GUI Professional Acceptance passed for Transformer / CustomerStation drag, Pole / RingCabinet regression, CableTermination orbit, GroundingPoint offset, valid-invalid-valid continuation, invalid release, NoChange, Cancel, group rollback, and expected-invalid non-modal behavior. Rendering.Wpf, Desktop, and full solution verification passed.
- Slice B introduced no route hysteresis, `RouteFamilyKey`, route continuity cache, switching margin, Transformer / CustomerStation obstacle policy, router rewrite, generic collision or overlap / bounds legality. The approved continuity and device-obstacle decisions are now frozen exclusively in Slice C; generic collision and overlap / bounds legality remain excluded.

### WP-EM-08 Slice C Closure Evidence

- Status: **Closed / Accepted / Implemented / Windows Verified / Professionally Accepted**. WP-EM-08 and WP-EM-09 are closed; the final WP-EM-09 implementation baseline is `bd8147956a0c4fba0acb0b47067c64f819d2507d`.
- Characterization reproduced a legal route-family flip with start `(0, 50)` / `Right`, end `(100, y)` / `Left`, obstacle `(40, 35, 20, 30)`, `ObstacleClearance = 4`, and `PortStubLength = 8`: `y = 49.9` selects the upper route while `y = 50.1` selects the lower route, producing an approximately 38 mm waypoint jump.
- `RouteFamilyKey` and hysteresis / switching margin are required. They are routing/runtime internal, transient-only, scoped independently by `ConnectionId`, and never Domain, Persistence, Command, or Undo / Redo facts. The key represents topology identity rather than exact waypoint coordinates. Implementation calibrated the numeric margin to 8 mm in document-space / routing-score behavior and verified it with tests and Windows visual verification.
- Current hard routing legality and ranking priority remain authoritative. Illegal candidates are discarded before continuity comparison; continuity cannot preserve an obstacle-invalid route, violate required waypoint / stub or no-backtracking constraints, override an explicit guide, or defeat a clearly superior higher-priority route.
- Continuity state lives for one drag transaction. Only a complete successful scene build publishes accepted family state; expected-invalid candidates do not pollute it. Layout, Scene, `LastValid`, and continuity snapshots roll back consistently. Release, NoChange, Cancel, and unexpected failure clear transient state. Group move maintains independent state per affected `ConnectionId` and rolls back all-or-none.
- Unrelated Transformer and CustomerStation professional body envelopes become hard routing obstacles. Labels, hit-test / selection padding, and other pure UI extents are excluded. A connection endpoint owner excludes its own obstacle by stable owner identity, not by testing whether an anchor lies inside bounds. Obstacle routing does not create generic drag-collision illegality; the router first attempts a legal bypass, and only a formal typed routing failure enters the existing Slice B expected-invalid path.
- The shared continuity mechanism covers `DeviceDragController`, `GroundingPointDragController`, and `CableRouteDragController`; controllers may provide lifecycle hooks but must not duplicate family classification, selection, or hysteresis. Undo / Redo and Save / Open rebuild deterministically from formal geometry, guides, and obstacles without transient route-family history.
- Required regression includes single- and multi-obstacle continuity, symmetry and threshold crossing, return path, illegal-current-family and clearly-superior switching, Transformer / CustomerStation bypass and endpoint-owner exclusion, overlapping unrelated obstacles, real OHL + GAP required waypoint / stub / no-backtracking / `RoutingConstraintException`, Slice B sequences, group rollback, all three controllers, transient cleanup, Undo / Redo, Save / Open, and existing guide / crossing / overlap ranking. FormatVersion remains V7 and persistence impact is none.
- Implementation, Windows verification, and professional acceptance passed. The final implementation chain includes routing continuity commit `e39c1cfe73d9b738e80789b9bc6efaed527ff160`, Clipboard integration Fix-1 `08b9623548ec89ab3f585c88a816d7a56e6ea321`, and Windows Fix-2 expectation correction `1cb55f358e5ce18026d7ddf8911e4f67f61c05c7`.
- Windows verification also closed the Clipboard integration regression introduced by hard Transformer / CustomerStation obstacles: automatic `Paste` uses deterministic bounded fallback, retries only `RoutingConstraintException`, rematerializes each candidate, atomically rolls back failed candidates, leaves no Domain / Layout / Selection / History residue, advances beyond the last successful automatic distance, keeps `PasteAt` from moving the automatic sequence backward, resets the cascade on new `Copy`, and preserves Undo / Redo.
- Professional acceptance result: **PASS**. Accepted behavior includes visually stable drag routes without unacceptable jitter or mouse-up route flip, formal-geometry-driven route switching, effective `CableRouteGuide`, `LastValid` preservation for expected-invalid drags, acceptable Transformer / CustomerStation obstacle routing, acceptable Clipboard fallback / repeated Paste behavior, and acceptable Undo / Redo.

### WP-EM-05 Closure Evidence

- Implementation and routing-fix baselines: `be7f2bd29875a112f01b8c93b7c85df6927b8871`, `17a1dba489bcf37a21693d6b55df653153979002`, `3d35ed30909cfb8e57ccabca093431e178cc7627`, `ebd062ae876e85d7581ba8ebe3b894fd66b1b835`, and `4f6b314a883d29b9c9afa1a385b5ec4c406809dc`.
- GAP interaction, snap / clamp / layout, Pole CableTermination manual routing, RingCabinet default / below-terminal routing, GroundingPoint drag, Undo / Redo, V7 persistence, and Canvas / PNG shared scene passed review and Windows acceptance.
- Windows professional acceptance: **Passed with Known Limitation**.
- Accepted Known Limitation (Deferred): RingCabinet cable-side GroundingPoint manually dragged above the cable terminal may still produce visual interference between the grounding leader and the interval internal vertical lead. This is presentation-only; it does not change electrical facts, `GroundingTarget`, `RingCabinetLayout`, interval geometry, or Terminal geometry. Default / below-terminal placement, GAP, and Pole CableTermination behavior remain accepted. A future option may constrain the symbol outside the upper terminal region, but that option is not implemented or frozen as a requirement.

### WP-EM-03 Closure Evidence

- `LoadSwitchInterval` and `IntegratedFeederInterval` support cable-terminal present / absent; `PTInterval` does not support the optional-terminal operation.
- Cable / Connection, `GroundingPoint`, and `WorkScope` dependency protection remains enforced.
- Undo / Redo preserves the cable-terminal identity contract.
- V7 nullable `CableTerminalId` persistence and V6 → V7 migration compatibility are complete.
- Clipboard mixed terminal presence is supported.
- An absent terminal produces no triangle, terminal anchor, or cable target; the interval internal lead remains rendered.
- Windows build, automated tests, and manual validation passed.

### WP-EM-04 Closure Evidence

- Implementation baseline: `b15166c96bcf03a38acd2a27d98b597d04b60d4d` (`fix(grounding): use local cable termination location`).
- Implementation and focused reviews passed; final Windows runtime validation reported all discovered tests passed with failed = 0 and skipped = 0.
- Domain.Tests: 113/113 passed; Infrastructure.Tests: 80/80 passed; full solution build passed.
- GUI acceptance passed for mounted-switch pole movement, GAP and mounted-switch coexistence, vertical GAP half-edge presentation, RingCabinet and local CableTermination GroundingPoint locations, and Lxx/Sxx numbering.
- FormatVersion remains V7; no migration was added.
- Grounding-specific GAP / GroundingPoint presentation continuity during legal route rebuilds is assigned to WP-EM-05. Generic pole/device drag route continuity and route-family hysteresis, illegal-geometry last-valid-position, and continuous-drag non-modal feedback are assigned to WP-EM-08.
